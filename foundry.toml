[profile.default]
src = "contracts"
out = "out"
libs = ["node_modules"]

# Hedera Testnet (used by scripts/deploy-hedera-testnet.sh; set HEDERA_RPC_URL in env)
[rpc_endpoints]
testnet = "${HEDERA_RPC_URL}"
